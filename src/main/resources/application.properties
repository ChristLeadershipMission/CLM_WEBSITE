spring.config.import=secret.properties

#db configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Mail Configuration
spring.mail.protocol=smtp
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.host=smtp.gmail.com
spring.mail.port=465


##Render environment variables
##Db Configuration
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.url=${DB_URL}
##Mail Configuration
#spring.mail.username=${MAIL_CONFIG_USERNAME}
#spring.mail.password=${MAIL_CONFIG_PASSWORD}
#jwt.signing.secret = ${JWT_SIGNING_SECRET_KEY}
#clmwebsite.minister.defaultCoordinatingMinisterId=${DEFAULT_COORDINATING_MINISTER_ID}
#clmwebsite.minister.defaultCenterCampusId=${DEFAULT_CENTER_CAMPUS_ID}
